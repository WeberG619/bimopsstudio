# Keep text files as LF in repo and on checkout (no Windows conversion)
* text=auto eol=lf

# If you ever add Windows scripts, keep them CRLF:
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
